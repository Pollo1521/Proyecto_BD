@model IEnumerable<Proyecto_BD.Models.EstatusCita>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Estatus_Cita)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Estatus_Cita)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Estatus_Cita">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Estatus_Cita">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Estatus_Cita">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
