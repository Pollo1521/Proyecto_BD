@model IEnumerable<Proyecto_BD.Models.CitasHorario>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hora_Cita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JornadaHorario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Hora_Cita)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JornadaHorario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Horario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Horario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Horario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
