@model IEnumerable<Proyecto_BD.Models.Recepcionista>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Jornada)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Usuario.Apellido_Materno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Jornada.ID_Jornada)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Recepcionista">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Recepcionista">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Recepcionista">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
