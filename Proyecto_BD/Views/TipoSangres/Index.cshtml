@model IEnumerable<Proyecto_BD.Models.TipoSangre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipo_Sangre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo_Sangre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Tipo_Sangre">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Tipo_Sangre">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Tipo_Sangre">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
